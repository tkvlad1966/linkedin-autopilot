{
  "name": "linkedin-autopilot",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome Extension MV3 + React Dashboard SaaS for LinkedIn outreach automation",
  "workspaces": [
    "dashboard",
    "extension",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=dashboard",
    "dev:ext": "npm run dev --workspace=extension",
    "build": "npm run build --workspace=dashboard",
    "build:ext": "npm run build --workspace=extension"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
